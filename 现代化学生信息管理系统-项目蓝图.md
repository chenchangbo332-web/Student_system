# 现代化学生信息管理系统 - 项目蓝图

## 1. 项目定位

这是一个面向学校教务场景的学生信息管理系统。系统的核心目的不是做一个只会保存姓名和学号的练习程序，而是模拟学校中常见的学生、班级、课程、成绩和教务统计流程。

项目将分阶段实现：先用 Python 做可运行的终端版，再使用 JSON 保存数据；随后升级到 MySQL、FastAPI、网页界面、登录权限和 AI 成绩分析。每个阶段都能独立运行和展示。

最终展示目标：

```text
管理员登录系统
-> 管理学生、班级和课程
-> 录入与查询成绩
-> 查看统计报表
-> 使用自然语言查询成绩和生成学习建议
```

## 2. 项目目标与边界

### 2.1 最终目标

- 管理学校的学生基本信息。
- 管理班级、专业、年级和班主任信息。
- 管理课程、教师、学分和开课学期。
- 录入、查询、修改和删除学生成绩。
- 生成平均分、排名、及格率、挂科名单等报表。
- 通过 AI 提供自然语言查询和成绩分析。
- 使用 GitHub 保存完整开发过程，形成作品集项目。

### 2.2 第一版不做的内容

为了保证项目能完成，第一版暂时不做以下功能：

- 选课和退课。
- 缴费、宿舍、考勤、图书馆、通知系统。
- 多学校、多校区管理。
- 完整的教师和学生登录权限。
- 网页前端、MySQL、Docker、AI Agent。

这些功能不是不重要，而是必须等基础业务正确后再增加。

## 3. 用户角色与权限

### 3.1 第一版用户

第一版只实现管理员。管理员拥有所有基础管理权限。

### 3.2 后续用户

| 角色 | 主要权限 |
| --- | --- |
| 管理员 | 管理学生、班级、课程、成绩和统计报表 |
| 教师 | 查看负责课程、录入和修改成绩、查看课程统计 |
| 学生 | 查看个人信息、成绩和 AI 学习建议 |

### 3.3 权限原则

- 管理员可以管理全校数据。
- 教师只能操作自己教授课程的成绩。
- 学生只能查看自己的数据。
- AI 不能绕过权限直接修改数据。

## 4. 核心业务对象

系统由五个主要对象组成。

```text
班级 1 --- N 学生
学生 1 --- N 成绩
课程 1 --- N 成绩
教师 1 --- N 课程（后续）
```

成绩是连接学生和课程的对象：一个学生可以有多门课程成绩，一门课程也会有多名学生的成绩。

### 4.1 学生 Student

| 字段 | 含义 | 规则 |
| --- | --- | --- |
| student_id | 学号 | 必填、全校唯一 |
| name | 姓名 | 必填 |
| gender | 性别 | 可限制为男、女或其他规范值 |
| age | 年龄 | 正整数 |
| class_id | 班级编号 | 必须对应存在的班级 |
| major | 专业 | 第一版保留，后续可由班级带出 |
| phone | 联系电话 | 字符串，避免丢失前导零 |
| enrollment_date | 入学日期 | 推荐格式 YYYY-MM-DD |

说明：当前代码里使用 `class_name` 也可以继续练习；当班级模块完成后，应逐步改为 `class_id`，通过班级编号关联班级，而不是重复保存班级名称。

### 4.2 班级 Classroom

| 字段 | 含义 | 规则 |
| --- | --- | --- |
| class_id | 班级编号 | 必填、唯一，例如 C2024-01 |
| class_name | 班级名称 | 必填，例如大数据技术 2 班 |
| major | 所属专业 | 必填 |
| grade | 年级 | 例如 2024 |
| head_teacher | 班主任 | 可为空 |

班级人数不手动输入。它应该由学生数据自动计算：筛选 `class_id` 等于当前班级编号的学生数量。

### 4.3 课程 Course

| 字段 | 含义 | 规则 |
| --- | --- | --- |
| course_id | 课程编号 | 必填、唯一，例如 PY101 |
| course_name | 课程名称 | 必填 |
| teacher_name | 任课教师 | 第一版用姓名，后续关联教师账号 |
| credit | 学分 | 大于 0 的数字 |
| semester | 开课学期 | 例如 2025-2026-1 |

### 4.4 成绩 Score

| 字段 | 含义 | 规则 |
| --- | --- | --- |
| score_id | 成绩编号 | 可自动生成 |
| student_id | 学号 | 必须对应已存在学生 |
| course_id | 课程编号 | 必须对应已存在课程 |
| score | 分数 | 必须在 0-100 之间 |
| exam_type | 考试类型 | 例如平时、期中、期末、总评 |
| semester | 学期 | 例如 2025-2026-1 |

一名学生在同一学期、同一门课程、同一种考试类型下只能有一条成绩记录。

### 4.5 用户 User（后续）

| 字段 | 含义 |
| --- | --- |
| user_id | 用户编号 |
| username | 登录用户名 |
| password_hash | 加密后的密码 |
| role | admin、teacher 或 student |
| related_id | 关联的学生或教师编号 |

密码不能以明文保存。当前终端练习阶段不实现登录；进入 FastAPI 阶段再做。

## 5. 功能模块

### 5.1 学生管理

功能：

- 新增学生。
- 按学号查询学生。
- 按姓名查询学生。
- 查看全部学生。
- 修改学生基本信息。
- 删除学生。
- 检查学号是否重复。
- 保存和读取学生数据。

业务规则：

- 新增前必须校验学号唯一。
- 查询或修改优先使用学号，不使用姓名作为唯一依据，因为同名学生可能存在。
- 删除学生前，后续应检查该学生是否存在成绩记录；先删除成绩或给出提示。
- 年龄必须能转换为整数，输入错误时不能让程序崩溃。

### 5.2 班级管理

功能：

- 新增班级。
- 按班级编号查询班级。
- 查看全部班级。
- 修改班级信息。
- 删除班级。
- 查看班级学生列表。
- 自动统计班级人数。

业务规则：

- 班级编号不能重复。
- 班级仍有学生时不能直接删除，应提示先转移或删除学生。

### 5.3 课程管理

功能：

- 新增课程。
- 查询课程。
- 查看全部课程。
- 修改课程。
- 删除课程。
- 查看某门课程的成绩列表。

业务规则：

- 课程编号不能重复。
- 课程已存在成绩时不能直接删除，避免留下无归属成绩。

### 5.4 成绩管理

功能：

- 录入成绩。
- 按学号查询某学生全部成绩。
- 按课程编号查询课程成绩。
- 修改成绩。
- 删除成绩。
- 保存和读取成绩数据。

业务规则：

- 学号和课程编号必须存在。
- 分数只能在 0-100。
- 不允许重复录入相同学生、相同课程、相同学期、相同考试类型的成绩。

### 5.5 统计报表

功能：

- 学生个人平均分。
- 课程平均分。
- 课程最高分和最低分。
- 课程及格率。
- 学生总评排名。
- 不及格学生名单。
- 班级平均分对比。

统计规则必须明确。例如：60 分及以上为及格；没有成绩的数据不参与平均分计算。

### 5.6 AI 成绩分析

AI 是最后添加的辅助功能，而不是系统核心数据来源。

示例问题：

```text
找出 Python 课程不及格的学生。
张三本学期平均分是多少？
哪门课程挂科人数最多？
请分析 20240001 的成绩变化，并给出学习建议。
```

AI 处理流程：

```text
用户自然语言问题
-> 判断问题类型
-> 调用已写好的查询或统计函数
-> 获得受控的真实数据
-> 组织结果或生成学习建议
```

安全原则：

- AI 只能调用预定义函数，例如 `find_failed_students(course_id)`。
- AI 不直接执行任意 SQL。
- AI 不直接改学生或成绩数据。
- AI 的建议要标注为辅助建议，不能当作真实教务结论。

## 6. 当前代码与推荐结构

当前练习阶段保留简单目录，便于学习面向对象和模块导入。

```text
Student_system/
├── app.py                         # 主菜单和程序入口
├── requirements.md                # 本项目蓝图
├── README.md                      # GitHub 项目说明
├── .gitignore
│
├── students/
│   ├── __init__.py
│   ├── student.py                 # Student 数据类
│   └── student_manager.py         # StudentManager 管理类
│
├── classes/
│   ├── __init__.py
│   ├── classroom.py               # Classroom 数据类
│   └── class_manager.py           # ClassManager 管理类
│
├── courses/
│   ├── __init__.py
│   ├── course.py                  # Course 数据类
│   └── course_manager.py          # CourseManager 管理类
│
├── scores/
│   ├── __init__.py
│   ├── score.py                   # Score 数据类
│   └── score_manager.py           # ScoreManager 管理类
│
├── reports/
│   ├── __init__.py
│   └── report_manager.py          # 统计函数
│
├── ai_analysis/
│   ├── __init__.py
│   └── score_analysis.py          # AI 调用已有统计功能
│
├── data/                          # JSON 数据文件，后续加入
│   ├── students.json
│   ├── classes.json
│   ├── courses.json
│   └── scores.json
│
└── tests/                         # 后续加入自动测试
    ├── test_student_manager.py
    ├── test_class_manager.py
    └── test_score_manager.py
```

### 6.1 每个文件的职责

```text
student.py
    只表示“一名学生有什么属性”，不负责 input、print 菜单或保存文件。

student_manager.py
    管理“许多学生”，负责新增、查询、修改、删除和业务校验。

app.py
    负责显示菜单、读取用户输入、调用 Manager 的方法。

data/*.json
    只负责保存数据，不写业务逻辑。
```

同样的规则适用于班级、课程和成绩模块。

## 7. 代码调用关系

终端阶段：

```text
用户
-> app.py 菜单
-> StudentManager / ClassManager / CourseManager / ScoreManager
-> Student / Classroom / Course / Score
-> JSON 文件
```

进入 Web 阶段后：

```text
浏览器页面
-> FastAPI 路由
-> Service 业务层
-> Repository 数据访问层
-> MySQL
```

说明：当前不需要马上创建 `Service` 和 `Repository` 目录。等终端版业务完整后再重构，才能真正理解为什么要分层。

## 8. 完整开发路线

### 阶段 0：需求与项目初始化

目标：明确系统做什么，GitHub 仓库能保存每次开发。

完成标准：

- 有需求文档。
- 有 README。
- 有 `.gitignore`。
- GitHub 仓库已关联并能 Push。

### 阶段 1：学生管理最小闭环

目标：管理员能完成学生的增删改查，关闭程序后数据不丢失。

任务顺序：

1. 完成 `Student` 类。
2. 完成新增学生。
3. 校验重复学号。
4. 修复按姓名查询，后续增加按学号查询。
5. 完成修改学生。
6. 完成删除学生。
7. 完成查看全部学生。
8. 使用 JSON 保存学生数据。
9. 启动时读取 JSON 数据。
10. 给每个功能手动测试。

验收案例：

```text
添加新学号 20240020 成功。
再次添加 20240020 被拒绝。
查询张三显示正确数据。
修改张三班级和电话后，查询能看到新值。
删除测试学生后，查询不到该学生。
退出并重新启动，已保存的数据仍存在。
```

### 阶段 2：班级管理

目标：班级成为独立对象，学生通过 `class_id` 关联班级。

任务顺序：

1. 建立 `Classroom` 类。
2. 建立 `ClassManager`。
3. 实现班级增删改查。
4. 为学生关联班级编号。
5. 根据学生数据统计班级人数。
6. 删除班级前检查是否仍有学生。

### 阶段 3：课程管理

目标：课程成为独立对象，为成绩模块提供课程信息。

任务顺序：

1. 建立 `Course` 类。
2. 建立 `CourseManager`。
3. 实现课程增删改查。
4. 增加课程编号重复校验。
5. 保存和读取课程数据。

### 阶段 4：成绩管理

目标：将学生和课程关联起来，完成学校系统最重要的教务数据。

任务顺序：

1. 建立 `Score` 类。
2. 建立 `ScoreManager`。
3. 录入成绩前检查学生和课程是否存在。
4. 校验成绩为 0-100。
5. 校验成绩记录不重复。
6. 支持按学生和课程查询成绩。
7. 支持修改与删除成绩。
8. JSON 保存和读取成绩。

### 阶段 5：统计报表

目标：让数据产生价值。

任务顺序：

1. 计算单个学生平均分。
2. 计算单门课程平均分。
3. 计算课程及格率。
4. 显示最高分、最低分。
5. 生成学生排名。
6. 输出不及格名单。
7. 对比班级成绩。

### 阶段 6：工程化升级

目标：从终端练习项目升级为可展示的真实后端项目。

顺序：

```text
JSON
-> MySQL
-> SQLAlchemy
-> FastAPI
-> 接口文档
-> 前端页面
-> 登录与权限
-> Docker
-> 部署
```

### 阶段 7：AI 功能

目标：在可靠数据和统计能力上加入 AI 应用开发展示点。

先实现固定问题，例如：

- 查询某课程不及格名单。
- 查询某学生平均分。
- 生成某学生的成绩总结。

再实现自然语言到函数调用的映射。不要让 AI 自由生成 SQL 或任意修改数据。

## 9. 当前阶段的技术要求

### 9.1 Python 基础

需要掌握：

- 类、对象、构造方法和 `__str__`。
- 列表、字典、循环和条件判断。
- 函数参数与返回值。
- 模块导入。
- 文件读写。
- 异常处理。

### 9.2 Git 与 GitHub

每完成一个可运行的小功能都执行：

```text
修改代码
-> 运行测试
-> git status
-> git add .
-> git commit -m "说明本次完成内容"
-> git push
```

推荐提交信息：

```text
完成学生类
增加学号重复校验
修复学生查询逻辑
完成学生信息修改功能
使用 JSON 保存学生数据
完成班级管理基础功能
```

### 9.3 测试原则

每个功能至少测试两类情况：

- 正常情况：输入正确数据，功能成功。
- 异常情况：重复学号、错误分数、不存在的编号，程序给出提示但不崩溃。

## 10. 当前项目的近期任务

当前只聚焦学生模块，暂不继续写空的班级和课程文件。

### 当前待办顺序

2. 后面版本待办:用学号而不是姓名作为修改和删除的主要查询条件。
4. 将学生数据保存为 JSON，不再使用 `str()` 和 `eval()`。
5. 启动程序时加载 JSON 数据。
6. 退出系统前先保存，再退出。
8. 在 README 中写运行方法和功能说明。

## 11. 当前代码的注意事项

- 不要把输入、菜单、数据保存全部堆在数据类中。
- `Student` 只保存学生属性；`StudentManager` 处理管理规则；`app.py` 处理菜单。
- 找到数据后使用 `return` 结束函数，避免继续执行“未找到”的提示。
- `break` 只退出循环，函数仍会继续往下执行。
- 不要使用 `eval()` 读取用户或文件数据，后续改为 `json.load()`。
- `phone`、`student_id`、`class_id`、`course_id` 建议用字符串保存。
- 每次新加功能前，先写出输入、处理、输出和错误情况。

## 12. 项目成功标准

终端版成功标准：

- 学生、班级、课程、成绩四个模块完整可运行。
- 数据关闭后重新运行仍存在。
- 常见错误输入不会让程序崩溃。
- 有统计报表。
- GitHub 有连续、清晰的提交历史。
- README 能让别人按步骤运行项目。

升级版成功标准：

- 使用 MySQL 保存数据。
- FastAPI 提供清晰接口。
- 有网页管理界面。
- 有管理员、教师、学生权限。
- AI 可以调用安全的查询和统计能力。
- 可以使用 Docker 启动。

